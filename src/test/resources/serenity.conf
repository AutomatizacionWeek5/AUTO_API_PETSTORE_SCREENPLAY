serenity {
  project.name = "PetStore API - Ciclo Completo CRUD"
  take.screenshots = "DISABLED"
}

# URL base de la PetStore Swagger (referencia; usada explícitamente en CallPetStoreApi)
restapi {
  baseUrl = "https://petstore.swagger.io/v2"
  logging = true
}
